{
  "name": "youyou-analytics-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute youyou_analytics --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute youyou_analytics --local --file=./schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.9.2",
    "wrangler": "latest"
  }
}
